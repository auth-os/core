pragma solidity ^0.4.23;

import "../core/Contract.sol";
import "./InitRegistry.sol";
import "./features/Provider.sol";

library Registry {

  function registerApp(address app, bytes32 name) external view {

  }

  function registerAppVersion(address last, address latest, bytes32 app_name, bytes32 version_name) external view {

  }
}
