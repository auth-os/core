pragma solidity ^0.4.23;

import "../Registry.sol";

library Provider {

  // Registers an application under a given name for the sender
  function registerApp(address _app, bytes32 _name) internal view {

  }

  // Registers a version of an application under a given name for the sender
  function registerAppVersion(address _last_version, address _current_version, bytes32 _app_name, bytes32 _version_name) internal view {

  }
}
